import React from 'react';

// TOFIX: use api for list json
import listJSON from '../../../sample-list.json';

const List = () => {
  console.log(listJSON);

  return <div>Placeholder for Genre List</div>;
};

export default List;
